<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="web.dao.face.GroupDao">
	
	<!-- 사용하는 테이블 정리 -->
	
	<sql id="selectBoard">
		SELECT 
		board_no, title, location
		, post_date, hit, content
		, delete_status, user_id, board_cate, (SELECT user_nick FROM userTB WHERE user_id=boardtb.user_id)
		FROM boardtb
		WHERE board_cate = 4
	</sql>
	
	<sql id="selectGroup">
		SELECT
			board_no, recruit_status
		FROM grouptb
	</sql>
	
	<sql id="selectRecom">
		SELECT
			userId, recoNo, boardCate
		FROM recomtb
	</sql>
	
	<sql id="selectUser">
		SELECT
			USER_ID
				user_pw, email, user_name
				,user_nick, birth, address
				,address_detail, phone, profile
				,join_date, user_status
		FROM usertb
	</sql>
	
	
	<!-- 1. 게시판 목록 조회 -->
	
    <select id="selectCntAll" resultType="int" parameterType="int">
    SELECT count(*) FROM (
    <include refid="selectBoard" />
    )
	</select>
		
    <select id="selectBoardList" resultType="BoardTb" parameterType="web.util.Paging" >
		SELECT * FROM (
		  SELECT 
		    ROWNUM AS rnum,
		    user_nick
		  FROM (
		    SELECT 
		      (SELECT user_nick FROM userTB WHERE user_id = boardTB.user_id) AS user_nick
		    FROM 
		      boardTB
		    WHERE 
		      board_cate = 4
		    ORDER BY 
		      board_no DESC
		  )
		) WHERE rnum BETWEEN #{startNo} AND #{endNo}
    </select>


	<!-- 2. 게시판 상세 조회 -->

	<update id="updateHit" parameterType="BoardTb">
		UPDATE boardtb
		<set>
			hit = hit + 1,
		</set>
		<where>
			AND board_no = #{boardNo}
		</where>
	</update>
	
	<select id="selectByBoardNo" resultType="BoardTb" parameterType="BoardTb">
		<include refid="selectBoard" />
		<where>
			AND board_no = #{boardNo}
		</where>
	</select>
	
	<!-- 유저 정보 중 유저 닉네임 가져오기 -->
	<select id="selectByUserNick" resultType="UserTb" parameterType="web.util.Paging" >
		SELECT * FROM (
		  SELECT 
		    ROWNUM AS rnum,
		    user_nick
		  FROM (
		    SELECT 
		      (SELECT user_nick FROM userTB WHERE user_id = boardTB.user_id) AS user_nick
		    FROM 
		      boardTB
		    WHERE 
		      board_cate = 4
		    ORDER BY 
		      board_no DESC
		  )
		) WHERE rnum BETWEEN #{startNo} AND #{endNo}
    </select>
	



</mapper>
